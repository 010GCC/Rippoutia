# Changelog — Rippoutai (離魂体)

Play observations drive these notes. Newest first.

## 2026-09-04 — v0.2-next (playable slice polish)

### Play observations addressed
- README still described a June fluid-controls sandbox; the live `index.html` already shipped a full reconnection loop. Docs were drifting from the build.
- Shadows read as reactive props more than agents with inner life (lean/eyes only).
- Players who slip a hold need recovery language, not silence.
- Cube “states” existed as payoffs but were not named or contrasted with a restless baseline.

### Changes
- **Named cube states**: `dormant` · `discord` · `weave` · `ring` · `ember` (HUD label + discord beam shiver).
- **Shadow agency**: velocity *echo* tilt; twins *merge/split* with alignment; non-focused souls *avoid* while another is near hold.
- **Recovery**: hold-slip messaging; **Space** explores a half-remembered unreconnected angle (off-center, not an auto-solve).
- **Onboarding hints** extended for slip recovery and Space.
- README synced to the actual build; playtest questions added.

### Still open (see README)
- Final-direction art vs procedural silhouettes.
- Deeper merge/split / echoing choreography.
- Broader playtest instrumentation.

## Earlier — fluid controls → reconnection loop (pre-changelog)

Carried forward into the current `index.html`:
- Spring-damper multi-axis rotation (park-anywhere).
- Three resonance orientations with distinct impossible payoffs.
- Title → play → hold-to-reconnect → finale → play again.
